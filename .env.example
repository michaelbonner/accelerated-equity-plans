# Zoho CRM Web Form Credentials
# Get these from: Zoho CRM → Setup → Channels → Webforms → Your Form → Embed Code

# The xnQsjsdp value from the form's hidden fields
ZOHO_WEBFORM_ID=your_form_id_here

# The xmIwtLD value from the form's hidden fields
ZOHO_WEBFORM_KEY=your_form_key_here

# The form action URL (usually https://crm.zoho.com/crm/WebToLeadForm or similar)
ZOHO_WEBFORM_URL=https://crm.zoho.com/crm/WebToLeadForm

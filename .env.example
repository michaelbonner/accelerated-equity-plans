ZOHO_CONTACT_FORM_URL=

# AWS — used for job application file uploads (S3) and submission storage (DynamoDB)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=

# Email — Mailpit for local dev (run: docker compose up -d)
# Update SMTP_HOST/PORT for production (e.g. SES, SendGrid, etc.)
SMTP_HOST="localhost"
SMTP_PORT="1025"
NOTIFICATION_EMAIL="info@acceleratedep.com"

# Turnstile
TURNSTILE_SITE_KEY="1x00000000000000000000AA"
TURNSTILE_SECRET_KEY="1x0000000000000000000000000000000AA"